# Pickty 진행 보관 — 2026-03-29

요약본. 루트 **`PROGRESS.md`** 가 최신 동기화 본문이다.

## P0 검증 정합 (프론트·백엔드)

- **전역 예외**: `GlobalExceptionHandler` — `MethodArgumentNotValidException`·`ConstraintViolationException`·`PicktyValidationException` → UTF-8 `text/plain` 400, DTO `message` 한글 노출.
- **티어 결과 PATCH**: `JsonNode` 병합 유지 + `Validator.validateProperty` + `UpdateTierResultMetaRequest` 프로브, 허용 키 `title`·`description`만.
- **템플릿·결과 길이**: 제목류 **100자**, 설명류 **10000자** — DTO·Zod·입력 `maxLength` 정합 **(2026-03-29 후반)**.

## 템플릿 UGC (소프트 삭제·메타 PATCH·파생)

- **엔티티**: `TemplateStatus` ACTIVE/DELETED, `TierTemplate.status` + **`@ColumnDefault('ACTIVE')`**.
- **목록**: `findAllByStatusOrderByCreatedAtDesc(ACTIVE)` — 삭제분 미노출.
- **단건 GET**: 삭제된 템플릿도 조회(기존 티어 결과·OG 정합).
- **DELETE**: 소프트(이미 삭제 시 idempotent), 409·하드 삭제 조건 **폐기**.
- **PATCH** `UpdateTemplateMetaRequest`: 제목·설명만(JSONB `items` 배열 불변). `SecurityConfig`에 **PATCH** 허가.
- **프론트**: `editTemplate`/`PUT` **제거** · `patchTemplateMeta` · `template-edit-meta-modal` · `/templates`·`/tier` 헤더 케밥 · **`forkTemplateId`** + 저장 시 `parentTemplateId` · 삭제 모달 문구(소프트 안내).
- **파생 UI**: 문구 **「이 템플릿을 바탕으로 새 템플릿 만들기」** — **`tier-board`** **저장\|다운로드** 버튼 **왼쪽** 배치.
- **DB 스크립트**: `docs/migrations/2026-03-29-tier-templates-status-default.sql` (기존 행·DEFAULT·NOT NULL).

## `/templates` 카드 UI **(2026-03-29)**

- 설명 블록 **두 줄 분량 고정 높이** (`min-h-[calc(2*0.875rem*1.375)]`), 푸터 **`h-11`** 고정으로 **아이템 n개·케밥** 줄 높이 안정화.

## 다음에 할 일 (사용자 메모)

- **「이 템플릿을 바탕으로 새 템플릿 만들기」** — 카피·시각·문구 등 **디테일** 수정.

## 구버전 메모 (당일 이전 스냅샷)

- 당일 초반 문서에는 **PUT 전체 수정·409 하드 삭제**가 적혀 있었음 → 위 UGC 설계로 **대체**됨.
